import { Route } from '@angular/router';
import { IncidentsComponent } from './admin-incidents/admin-incidents.component';

export const adminIncidentsRoutes: Route[] = [
  { path: '', component: IncidentsComponent },
];

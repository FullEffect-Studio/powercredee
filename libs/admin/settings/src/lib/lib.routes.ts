import { Route } from '@angular/router';
import { SettingsComponent } from './admin-settings/admin-settings.component';

export const adminSettingsRoutes: Route[] = [
  { path: '', component: SettingsComponent },
];
